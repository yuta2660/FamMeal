import React from "react";
import {
  BrowserRouter as Router,
  Routes,
  Route,
} from "react-router-dom";

function App() {
  return (
    <Router>
      <Routes>
        {/* ville作成ページ */}
        {/* <Route path="/" element={<Grid />} /> */}
      </Routes>
    </Router>
  );
}

export default App;
